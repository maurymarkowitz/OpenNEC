Items still to be implemented:

* does anything in card.c really need ctx?

* ensure that the logic for optional placing the formulas in the onec comment is implemented, and that if it is used and the deck is written in nec-2 format, that the value is copied into the corresponding field

* remember to run clang static analyzer at some point

* replace alloc/free for error messages, just use a stack allocation

* add handlers for ITeration and OPtimization cards, which produce multiple output runs from a single deck. in onec these should be processed out into some sort of "run" or "simulate" object with any differences from the original definitions.

* add TIA-804-B (.adf) pattern output, which is simply a standard way of writing the NEC pattern

* add a -f(ormat) flag to the CLI version to specify an output format so it can write NEC, 4nec2, etc.

* re-consider ignore: `ignore` tells the system to skip over that card when producing geometry or running calculations. This is useful during testing, as cards can be turned off without having to remove them from the deck or comment them out.

* at some point, handle 4nec2 current sources, EX 6 and 7

* add support for LD type 7 from 4nec2

* revisit % handling in formulas and parsing - some files use % for percentages, like example3.nec

* files to examine:
- Nec-Ex9.nec, - no geometry
- coffee1.nec, - formula problems
- 2EL160H.nec, - has pf units
- Nec-Ex4.nec, Tank.nec, Horn.nec - no FR
- VE4MAfeed.nec - SP with type 3, but the next card is not an SC
- check k4ero-4.nec for missing taper, likely due to formula issue
- patch.c - why so many segments?
- Example3.nec - has 50% in one of the fields
- D2T.nec - what is "#0.4
- UA3SFH.nec, W2EEY.NEC, etc. - IMPROPER LOAD TYPE CHOSEN, REQUESTED TYPE IS 6
- calvin.nec, class.NEC - REQUESTED TYPE IS 7
- why does hrs_441.nec overflow the buffer set up in network.c around line 290?
- Gr_func.nec, NGF_radial4.nec - WG card