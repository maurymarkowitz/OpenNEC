--- src/deck.c.orig 2024-02-04 12:00:00.000000000 -0500
+++ src/deck.c  2024-02-04 12:00:00.000000000 -0500
@@ -782,6 +782,7 @@
       for(int u = 1; u < NUM_ONEC_UNIT_CODES; u++) {
         const char *unit_str = unit_codes[u];
         size_t len = strlen(sym->value);
         size_t ulen = strlen(unit_str);
+        fprintf(stderr, "  Checking u=%d '%s' (ulen=%zu) against '%s' (len=%zu): suffix='%s'\n", u, unit_str, ulen, sym->value, len, len >= ulen ? sym->value + len - ulen : "N/A");
         if(ulen > 0 && len >= ulen && strcmp(sym->value + len - ulen, unit_str) == 0) {
           unit = u;
